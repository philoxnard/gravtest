{% extends "main_template.j2" %}
{% block content %}

  {% include 'events/modals.j2' %}

  <!-- Masthead -->
  <header class="masthead">
    <div class="container h-100">
      <div class="row h-100 align-items-center justify-content-center text-center">
        <div class="col-lg-10 align-self-end">
          <a href="/event/{{ event.label }}"><h1 class="text-uppercase text-white font-weight-bold">{{ event.title }}</h1></a>
          <hr class="divider my-4">
        </div>
        <div class="col-lg-8 align-self-baseline">
          <h1>Confirmed!</h1>
          <p class="text-white-75 font-weight-light mb-5">{{ event.datetime }}</p>
          <p class="text-white-75 font-weight-light mb-5">{{ event.location }}</p>
        </div>
      </div>
    </div>
  </header>

  <!-- Event Section -->
  <section class="page-section" id="contact">

    <!-- Main Content -->
    <div class="container">
      <div class="row">
        <div class="col-md-4">
          <h2>You are confirmed to attend!</h2>
          <p>{{ event.description }}</p>
        </div>
      </div>
      <input id="event_id" value="{{ event.id }}" hidden>
    </div>

  </section>

  <script src="/js/events/events.js"></script>

{% endblock %}